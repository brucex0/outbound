# Bundled fallback audio

- Keep this pack intentionally small and reviewed in every supported locale.
- Source audio lives here; generation copies the same files into each platform bundle.
- Missing same-locale audio must remain silent rather than fall back to another language.
