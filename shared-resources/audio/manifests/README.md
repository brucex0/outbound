# Live-coach audio manifests

- Reviewed production audio remains in signed cloud packs described by `docs/live-coach-operations.md`.
- Store checked-in manifest fixtures here only when both clients consume the same versioned schema.
- Do not commit a full generated voice catalog.
